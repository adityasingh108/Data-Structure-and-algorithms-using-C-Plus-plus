#include<bits/stdc++.h>
using namespace std; 

using namespace std; 
#define ll long long int

int main()
{
    ll  t;
    cin >> t;
    while(t--){
        ll  n  , r;
        cin >> n >> r; 
        vector<ll> sweets(n);
        vector<ll> shops(r);

        for(ll i = 0; i < n; i++){
            ll ele;
            cin >>ele;
            sweets.push_back(ele);
        }
        for(ll i = 0; i < n; i++){
            ll ele;
            cin >>ele;
            sweets.push_back(ele);
        }


        
    }
    
    return 0;
}   