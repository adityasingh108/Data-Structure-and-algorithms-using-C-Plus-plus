#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        long long N, X, temp1, ans;
        cin >> N;
        vector<long long > vect(N);
        for(int i = 0; i< N; i++){
            cin >> vect[i];
        }
        
    }
    return 0;
}